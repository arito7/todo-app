import './header.css';
import React from 'react';

const Header = () => {
  return (
    <div className="header">
      <h1>Todo App</h1>
      <button>New Note</button>
    </div>
  );
};

export default Header;
